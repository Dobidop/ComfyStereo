[project]
name = "comfystereo"
description = "A node for stereoscopic image generation (sid-by-side, cross-eyed, cyan-side-by-side etc.) Nodes: Stereo Image Node - a basic port from the Automatic1111 stereo script, and additional modifications such as edge and direction aware blurring of depth map"
version = "1.0.0"
license = {file = "LICENSE"}
dependencies = ["torch", "numpy", "Pillow", "opencv-python"]

[project.urls]
Repository = "https://github.com/Dobidop/ComfyStereo"
#  Used by Comfy Registry https://comfyregistry.org

[tool.comfy]
PublisherId = "Dobidop"
DisplayName = "ComfyStereo"
Icon = ""
 
